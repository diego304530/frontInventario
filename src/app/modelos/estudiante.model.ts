export class Estudiante {
    _id?:string; // Identificador único del estudiante (opcional)
    cedula?:string; // Número de cédula del estudiante (opcional)
    nombre?:string; // Nombre del estudiante (opcional)
    apellido?:string; // Apellido del estudiante (opcional)
}
